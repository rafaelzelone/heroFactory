import { prismaMock } from "../__mocks__/prisma";
import { HeroService } from "../hero.service";

describe('Hero Unit Tests', () => {
  it('should have at least one test', async () => {
    expect(true).toBe(true);
  });
});